# Copied into the shell (or CI) before `sanity deploy`.
# Hosted Studio bakes these at deploy time.

SANITY_PROJECT_ID=lzbhll1u
SANITY_DATASET=production

# Stable Vercel site URL used by Presentation Tool preview iframe.
# Example (replace with current production alias from Vercel Domains):
# SANITY_STUDIO_PREVIEW_URL_ORIGIN=https://cloud-employee.vercel.app
SANITY_STUDIO_PREVIEW_URL_ORIGIN=http://localhost:3000
