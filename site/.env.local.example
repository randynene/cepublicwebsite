NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=
SANITY_API_READ_TOKEN=
NEXT_PUBLIC_SITE_URL=
NEXT_PUBLIC_SANITY_STUDIO_URL=

# C6 HubSpotFormEmbed (HALT 6 — Step 2.11). HubSpot Marketing portal ID.
# Customer 1 (Cloud Employee): 22809822. Optional — C6 component falls
# back to error state if unset; rest of design system builds without it.
NEXT_PUBLIC_HUBSPOT_PORTAL_ID=
# Fallback contact email shown in C6's error state when a HubSpot embed
# fails to load. Defaults to hello@cloudemployee.io if unset.
NEXT_PUBLIC_FALLBACK_EMAIL=
